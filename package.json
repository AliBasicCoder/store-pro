{
  "scripts": {
    "test": "npm --prefix ./store-pro run build-test && npm --prefix ./store-pro-react run build-test && npm --prefix ./store-pro-svelte run test && npm --prefix ./store-pro-angular run build-test"
  }
}
